language: node_js
node_js: 
  - "stable"
cache:
  directories:
    - "node_modules"
services:
  - postgresql
before_script:
  - psql -c "create user edu1 with password 'admin@123';" -U postgres
  - psql -c "create database stackoverflow;" -U postgres
  - psql -c "insert into users(name, email, password) values('Jeffery','admin@blog.com',secret);" -U postgres
  - npm run build
  - npm run migration
after_success:
  - npm run coveralls
